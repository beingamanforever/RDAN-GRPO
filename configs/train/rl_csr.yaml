defaults:
  - base
  - _self_

exp_name: rl-csr-qwen3-4b

# Baseline: one outcome channel, mean rubric satisfaction over all rubrics.
tracker_kwargs:
  tags: [qwen3-4b, instruction-following, hybrid-16k, rl-csr]

rdan_response:
  method: rl_csr
