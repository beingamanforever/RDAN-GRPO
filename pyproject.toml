[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rdan-grpo"
version = "0.1.0"
description = "A compact reward and advantage core for RDAN-GRPO experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["datasets==3.1.0", "openai==2.20.0", "pyyaml>=6", "torch>=2.1"]

[project.optional-dependencies]
dev = ["matplotlib>=3.8", "pytest>=8", "ruff>=0.11"]
plots = ["matplotlib>=3.8"]

[tool.ruff]
line-length = 119
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.data-files]
"share/doc/rdan-grpo" = ["THIRD_PARTY_NOTICES.md", "third_party/licenses/RTT-APACHE-2.0.txt"]
